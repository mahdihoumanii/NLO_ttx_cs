#pragma once

namespace ttbar_qtsub {

double born_qq(double shat, double mt, double mu, double gs,double c);
double born_gg(double shat, double mt, double mu, double gs, double c);

}  // namespace ttbar_qtsub
